[pytest]
testpaths = tests
# Quiet, with the assertion that failed shown in full. The catalogue tests
# compare against a transcribed copy of section 08, so a diff is the useful
# output when one fails.
addopts = -q --tb=short
